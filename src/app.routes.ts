
import { Routes } from '@angular/router';

export const routes: Routes = [
  // Add any routes here if the application grows to have multiple views.
  // For now, it's a single-page marketing site.
];
    